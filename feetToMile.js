function feetToMile(measuerment) {
    if (measuerment < 0) {
        alert("Your Input Is Invalid!");
       return calculation;
    }else{
        const calculation = (measuerment * 0.00019);
        return calculation;
    }
    // const calculation = (measurement * 0.00019);
    // return calculation;
}
const result = feetToMile(-500);
// const result = Math.round(input);
console.log( result);